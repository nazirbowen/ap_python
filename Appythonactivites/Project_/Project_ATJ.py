def profileData():
    
    name = input('whats your name ? ')
    Goal = float(input('what is your ideal income ? '))
    CurrentProfession = (input('What are things youre good at ? '))
    Location = (input('Where are you located ? '))
    PreviousApplied = (input('What are some places you have applied ? '))
    PreviouslyWorked = (input(' What are some places you have worked ? '))
    print('Welcome to Auto Job finder.')
    
profileData()